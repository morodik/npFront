server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # для React/Vite роутинга
    location / {
        try_files $uri $uri/ /index.html;
    }
}